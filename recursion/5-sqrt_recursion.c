#include "main.h"

/**
* _sqrt_recursion - entry point
* @n:
* Return: result
*/

int _sqrt_recursion(int n)
{
	if (y > 0)
	{
		return (_sqrt_recursion(
	}
	if (i < 0)
	{
		return (-1);
	}
	return (1);
}
